from . import authors, books, cart, categories, images, publishers, series, users

# docker-compose exec web python -m alembic upgrade head
# docker-compose exec web python -m alembic revision --autogenerate
